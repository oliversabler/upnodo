name: "Dependabot"

on:
  pull_request

jobs:
  dependabot:
    runs-on:
      version: 2
      updates:

        - package-ecosystem: "github-actions"
          directory: "/"
          schedule:
            interval: "daily"
